{
  "name": "redevtools",
  "version": "1.1.2",
  "description": "ReDevTools extends your browser devtools with custom plugins",
  "main": "dist/index.js",
  "homepage": "https://www.redevtools.com",
  "repository": "https://github.com/redevtools/redevtools",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && copyfiles \"./plugins/**/*.gif\" dist && copyfiles \"./plugins/*.json\" dist",
    "watch": "tsc --watch",
    "prepare": "npm run build"
  },
  "keywords": [],
  "author": "Buglink.com Team",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.0"
  }
}
